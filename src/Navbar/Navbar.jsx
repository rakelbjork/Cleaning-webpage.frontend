import React from 'react';
import { AppBar, ToolBar, IconButton, Menu, Typography }

const Navbar = () => {
    return (
        <div>
        <AppBar>
            <ToolBar>
                <Typography>
                    <img src={} alt="Städafint.js" height="27px" />
                    Städafint.js
                </Typography> 
            </ToolBar>
        </AppBar>
        </div>
    )
}

export default Navbar 